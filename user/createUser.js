"use strict";
// Only validate email and password.
// bcryp password
// add extra fields
// authentisera e-mail

module.exports = function(app) {
    app.post('/api/user/createUser', (req, resp) => {

    });
}
